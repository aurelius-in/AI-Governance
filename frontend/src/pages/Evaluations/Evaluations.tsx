import React from 'react';
import { Typography, Box } from '@mui/material';

const Evaluations: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Evaluations
      </Typography>
      <Typography>Model evaluation and testing</Typography>
    </Box>
  );
};

export default Evaluations;
